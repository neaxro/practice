services:
  nginx:
    image: nginx:1.29.0
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/certs:ro
      - ./html:/usr/share/nginx/html:ro
    ports:
      - 80:80
      - 443:443
